{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest",
    "lint": "eslint **/* --ext .ts"
  },
  "jest": {
    "preset": "ts-jest",
    "testPathIgnorePatterns": ["dist"]
  },
  "dependencies": {
    "axios": "^0.24.0",
    "mongoose": "^5.13.13"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "ts-jest": "^27.1.2",
    "typescript": "^4.0.0"
  }
}
