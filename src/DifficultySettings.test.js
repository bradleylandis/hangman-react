import { shallow } from "enzyme";
import React from "react";
import DifficultySettings from "./DifficultySettings";

describe("DifficultySettings", () => {
  it("has correct default maxLength", () => {
    // const wrapper = shallow(<DifficultySettings/>)
    //
    // const maxLengthInput = wrapper.find('input');
    //
    // console.log(maxLengthInput.html())
    //
    // expect(maxLengthInput.value).toBe(7)
  });
});
